- NEVER skip pre-commit hooks (no --no-verify, no -n). If a hook fails, fix the underlying issue or ask for help. Do not bypass hooks under any circumstances.
- Never open a GitHub issue as a result of debugging findings, in ANY repo, unless I explicitly ask for one. This applies especially to repos I don't own, above all Anthropic's own repos (e.g. `anthropics/claude-code`): when you hit a Claude Code, CLI, or tool bug/limitation, do NOT file it upstream. Report the finding here in the conversation and fix our own code if asked. I run in auto/bypass mode most of the time, so there is no prompt to stop you, this rule is the only guardrail. Filing an issue is outward-facing and public; treat it as never-without-explicit-ask.
- Always run Glean MCP tools (`mcp__glean_default__*`) and Slack MCP search/read tools (`mcp__plugin_slack_slack__*_search_*`, `mcp__plugin_slack_slack__*_read_*`) inside a subagent (Task tool) to avoid bloating the main context with large results. Summarize the findings when returning to the main conversation. Write/send tools (e.g. `slack_send_message`, `slack_schedule_message`) can be called directly.
- Do not add unnecessary comments to code. Favor intelligent variable, method, test case and file names over excessive comments. If a line of code is self-explanatory, let it speak for itself. However, if there is something unusual, unintuitive or complex about code, include a comment. Be sure to always include docstrings though.
- When I ask you to give me a prompt for a new session, also copy it to my clipboard with `pbcopy`.
- When using echo for visual spacers in chained commands, use `echo "==="` or `echo` (blank line) instead of `echo "---"` — the `---` pattern triggers the quoted-flag-name safeguard.
- Save brainstorming specs to `./specs/` and implementation plans to `./plans/` instead of `docs/superpowers/`. Do not commit them to git.
- When creating git branches, always use the `jeffdt/<domain>-<brief-kebab-description>` format. The `jeffdt/` prefix is mandatory. Domain is the feature area (e.g., `insights`, `auth`), description is short kebab-case. Drop filler words. Examples: `jeffdt/insights-add-cache-ttl`, `jeffdt/support-chat-verify-webhook`.
- Git worktrees are managed with `wt` (worktrunk). Use `wt switch --create <branch>` to create worktrees (sibling dir layout, e.g. `~/r/app.jeffdt-auth-fix`). Do not manually run `git worktree add`. Use `wt list` to see worktree status and `wt remove` to clean up.
- Never use em dashes (—) in text output, commit messages, PR descriptions, or any written content. Use commas, semicolons, colons, parentheses, or separate sentences instead.
- When referencing local files in conversation output, render them as clickable Markdown links using `file://` URIs: `[relative/path.md](file:///absolute/path.md)`. This lets Ghostty open the file in the user's editor on cmd+click. Derive the absolute path from the current working directory.
- When committing in the `~/.claude` repo, bundle everything in the working tree into a single commit. This repo is a config backup; do not ask to split changes by concern or scope the commit to just what the current session touched. The commit-to-main marker is intentional.
