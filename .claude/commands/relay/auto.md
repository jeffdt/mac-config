---
description: Activate relay-auto, clipboard commands AND auto-read another tab/window
argument-hint: [tab-title]
---

Activate **relay-auto** mode. Load `skills/relay-auto/SKILL.md` via the Skill tool and follow its activation steps.

If `$ARGUMENTS` is non-empty, treat it as a hint for the watched tab title; pass it to the skill's target-resolution step.
